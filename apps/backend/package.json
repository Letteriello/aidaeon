{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "predev": "convex dev --until-success",
    "build": "convex deploy --cmd 'bun run build:functions'",
    "build:functions": "tsc --noEmit",
    "deploy": "convex deploy",
    "codegen": "convex codegen",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .convex _generated"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.10.0",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "convex": "^1.25.4",
    "@node-rs/argon2": "^2.0.2",
    "@node-rs/bcrypt": "^1.10.0",
    "openai": "^4.20.0",
    "zod": "^3.22.0",
    "date-fns": "^3.0.0",
    "nanoid": "^5.0.0",
    "jose": "^5.2.0"
  }
}
